using System.Collections;
using System.Collections.Generic;
using DG.Tweening;
using UnityEngine;

namespace STELLAREST_2D.Test
{
    public class DoTweenTest : MonoBehaviour
    {
        private void Start()
        {
            // transform.DOMove(Vector3.up, 5f);
            // transform.DOScale(Vector3.one * 3, 5f);
            // transform.DORotate(Vector3.forward * 45f, 5f);

            // Material mat = GetComponent<SpriteRenderer>().material;
            // mat.DOFade(0f, 5f);
            
            // RectTransform rt = GetComponent<RectTransform>();
            // rt.DOAnchorPosY(0, 5).SetDelay(5f).SetEase(Ease.OutElastic);
            // rt.DOAnchorPosY(0, 5).SetDelay(5f).SetEase(Ease.OutBounce);
            // rt.DOAnchorPosY(0, 5).SetDelay(5f).SetEase(Ease.InBounce);
            // rt.DOAnchorPosY(0, 5).SetDelay(5f).SetEase(Ease.InOutBounce);

            // TMPro.TextMeshProUGUI text = GetComponent<TMPro.TextMeshProUGUI>();
            // text.DOFade(-1, 3f);
        }
    }
}
